<h2>This are your tasks today</h2>

<% @tasks.each do |task| %>
<h5> <%= task.done ? "[X]" : "[ ]" %> <%=task.name %> <%= link_to "open", task_path(task) %></h5>
<% end %>

<%= link_to "Add a task", new_task_path %>
